[phases.setup]
nixPkgs = ['php82', 'php82Extensions.pdo', 'php82Extensions.pdo_mysql', 'php82Extensions.mysqli', 'php82Extensions.mysqlnd']

[phases.install]
cmds = ['echo "PHP y extensiones MySQL instaladas correctamente"']

[start]
cmd = 'php -S 0.0.0.0:$PORT -t .'
